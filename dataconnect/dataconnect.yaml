specVersion: "v1beta"
serviceId: "flutter_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "flutter_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
